{
  "frontend/src/App.tsx": "Main app with TanStack Router, auth guard, profile setup modal, sidebar layout, and all routes; fixes `ProfileSetupModal` usage by removing unsupported `open`/`onClose` props (the modal manages its own open state internally).",
  "frontend/src/components/ActivitiesList.tsx": "Displays activities for a report as cards with status badges, hours, audience, and edit navigation; fixes import to use the renamed `useActivitiesForReport` hook.",
  "frontend/src/pages/ReportsListPage.tsx": "Lists reports with stats, filters, and role-based UI; fixes imports to use `statusLabel` (not `getStatusLabel`) and `MONTHS` from labels.",
  "frontend/src/pages/ActivityFormPage.tsx": "Complete activity form page with hybrid \"Horas dedicadas à atividade\" field (numeric input + \"Não se aplica\" checkbox with mutual-exclusion logic); fixed createActivity.mutateAsync to pass Activity directly and updateActivity.mutateAsync to use the correct { activityId, activity } shape.",
  "frontend/src/pages/DashboardPage.tsx": "Coordination/Admin dashboard with filter controls (month, team/museum using MUSEUM_LOCATIONS enum with proper MuseumLocation type, professional), KPI cards, status breakdown, and multiple Recharts visualizations; museum filter state is typed as MuseumLocation | 'all' to satisfy DashboardFilter type requirements.",
  "frontend/src/utils/pdfGenerator.ts": "Generates a formatted HTML document (opened in a new tab with auto-print) for a report and its activities; local getMuseumLabel maps all 6 MuseumLocation enum values including 'Equipe Principal' (renamed) and the 5 new team areas.",
  "frontend/src/utils/excelGenerator.ts": "Full CSV/Excel export generator; local getMuseumLocationLabel maps all 6 MuseumLocation enum values including 'Equipe Principal' and the 5 new team areas, with TOTAL row aggregating only numeric dedicatedHours values.",
  "frontend/src/components/ExportReportPDFButton.tsx": "Simplified PDF export button that calls `generateReportPDF` (which now returns `void` and opens a new tab directly) instead of handling a Blob return value.",
  "frontend/src/components/ExportConsolidatedExcelButton.tsx": "Button (visible only to Administration role) that exports all reports and activities as a CSV file; fixes imports to use the renamed `useAllReports` and `useAllActivities` hooks.",
  "frontend/src/utils/labels.ts": "Utility module with label mapping functions for all backend enums; adds `getStatusLabel` as an alias for `statusLabel` and exports a `MONTHS` array of ordered `Month` enum values to satisfy all import sites.",
  "frontend/src/components/SignatureCanvas.tsx": "Reusable HTML5 canvas component for electronic signature capture with mouse and touch support, a clear button, and an imperative handle to retrieve the base64 PNG signature.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; adds `useApproveUser` mutation hook that calls `actor.approveUser(user)` and invalidates `allUserProfiles`, `approvals`, and `isCallerApproved` query keys on success, keeping all existing hooks unchanged.",
  "frontend/src/pages/ReportFormPage.tsx": "Complete report form page supporting create/edit for professionals, coordination, and administration users; adds a separate `canEditActivities` variable so coordinators can always add/edit activities on any report (including reports they don't own), while keeping report fields read-only for coordinators viewing others' reports. Also updates the read-only info alert to mention that coordinators can still insert activities.",
  "frontend/src/components/Sidebar.tsx": "Navigation sidebar with role-based nav items; accepts an `isApproved` prop and hides all navigation items (except logout) for users who are not yet approved, preventing pending users from navigating to protected pages.",
  "frontend/src/components/ApprovalDetailView.tsx": "Read-only report detail view for Coordination/Admin with coordinator comments, signature canvas, and approve/return-for-revision actions; fixes the signature retrieval call from the non-existent `getSignature()` to the correct `getSignatureBase64()` method on `SignatureCanvasHandle`.",
  "frontend/src/utils/consolidatedPdfGenerator.ts": "Generates a consolidated PDF report for all reports and activities; local getMuseumLocationLabel maps all 6 MuseumLocation enum values including 'Equipe Principal' and the 5 new team areas, with correct hours aggregation.",
  "frontend/src/utils/consolidatedCsvGenerator.ts": "CSV generator for consolidated report export; local getMuseumLocationLabel maps all 6 MuseumLocation enum values including 'Equipe Principal' and the 5 new team areas, with TOTAL row summing only numeric hours.",
  "frontend/src/pages/ConsolidatedMuseumPage.tsx": "Consolidated museum report page for Coordination/Admin; fixes imports to use the renamed `useAllReports` and `useAllActivities` hooks, and uses `MUSEUM_LOCATIONS` from labels for the museum selector.",
  "frontend/src/pages/ApprovalsPage.tsx": "Coordination/Admin-only page listing all reports with filter controls and a detail view; fixes imports to use `statusLabel` (not `getStatusLabel`), `MONTHS` from labels, and passes `onClose` prop to `ApprovalDetailView`.",
  "frontend/src/components/dashboard/ReportsByMuseumChart.tsx": "Bar chart component visualizing report counts per museum using Recharts, with truncated X-axis labels, custom tooltip showing full museum name, and empty state handling.",
  "frontend/src/components/dashboard/ReportsByMonthChart.tsx": "Bar chart component visualizing report counts per month using Recharts, with months sorted chronologically using MONTH_ORDER, Portuguese month labels via getMonthLabel, and empty state handling.",
  "frontend/src/pages/UserManagementPage.tsx": "Coordination/Administration page for managing users; adds a `PendingUsersSection` component above the active users table that lists all users with `pending` approval status, each with an \"Aprovar\" button wired to the `useApproveUser` mutation with toast notifications, and shows an empty-state message when no pending users exist.",
  "frontend/src/components/ProfileSetupModal.tsx": "Dialog modal for first-time profile setup with name input and role selection; fixes the `saveProfile` call to include the required `museum: ''` field to satisfy the `UserProfile` type.",
  "frontend/src/pages/PendingApprovalPage.tsx": "Full-screen page shown to authenticated users whose accounts are awaiting coordinator approval; displays the Museus Centro logo, a clear pending-approval message with instructions, a re-send approval request button, and a logout button."
}