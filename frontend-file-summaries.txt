{
  "frontend/src/App.tsx": "Main app with TanStack Router, auth guard layout, profile setup modal, pending approval screen, sidebar layout, all routes, and a global `Toaster` (sonner) for toast notifications.",
  "frontend/src/components/ActivitiesList.tsx": "Activities list component with `reportId`, `activities`, `isLoading`, and `canEdit` props interface; renders activity cards with status badges and edit navigation.",
  "frontend/src/pages/ReportsListPage.tsx": "Reports list page showing user's reports with expand/collapse activities, edit/delete actions, and PDF export; now imports `Report` and `Activity` from `../types` so the `ExportReportPDFButton` receives the correct full `Report` type.",
  "frontend/src/pages/ActivityFormPage.tsx": "Complete activity form page with all fields; replaces the missing `useGetActivity` import (now correctly exported from useQueries) and uses it for loading existing activity data when editing.",
  "frontend/src/pages/DashboardPage.tsx": "Coordination/Admin dashboard with KPI cards and charts; uses `null` (not `undefined`) for DashboardFilter fields, and converts all bigint values to number via a `toNum` helper before passing to chart/KPI components.",
  "frontend/src/utils/pdfGenerator.ts": "Generates a formatted HTML document (opened in a new tab with auto-print) for a report and its activities; local getMuseumLabel maps all 6 MuseumLocation enum values including 'Equipe Principal' (renamed) and the 5 new team areas.",
  "frontend/src/utils/excelGenerator.ts": "CSV/Excel export generator with Portuguese column labels; adds `Impacto Esperado` column to the report-level headers and data rows, and includes the `generateConsolidatedExcel` convenience export.",
  "frontend/src/components/ExportReportPDFButton.tsx": "Simplified PDF export button that calls `generateReportPDF` (which now returns `void` and opens a new tab directly) instead of handling a Blob return value.",
  "frontend/src/components/ExportConsolidatedExcelButton.tsx": "Button (visible only to Administration role) that exports all reports and activities as a CSV file; fixes imports to use the renamed `useAllReports` and `useAllActivities` hooks.",
  "frontend/src/utils/labels.ts": "Utility module with label mapping functions for all backend enums; removes the deprecated `generalCoordinator` → 'Coordenador Geral' entry from `getRoleLabel`, keeping only the four valid `AppUserRole` values: coordination, coordinator, administration, and professional.",
  "frontend/src/components/SignatureCanvas.tsx": "Reusable HTML5 canvas component for electronic signature capture with mouse and touch support, a clear button, and an imperative handle to retrieve the base64 PNG signature.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; adds missing exports (`useListFiles`, `useGetReportsForUser`, `useDeleteReport`, `useGetReportWithActivities`, `useGetReport`, `useGetActivity`, `useGetTotalGeneralAudience`, `AudienceQueryType`), fixes `comment` type to `string | undefined`, fixes `useSubmitReport` signature to accept optional signature, and fixes `useActivitiesForReport` to accept `string | null | undefined`.",
  "frontend/src/pages/ReportFormPage.tsx": "Complete report form page with identification, narrative, opportunities, activities, and signature sections; fixes the `signatureRef` type by importing and using `SignatureCanvasHandle` from `SignatureCanvas`, resolving the ref type mismatch.",
  "frontend/src/components/Sidebar.tsx": "Navigation sidebar with role-based nav items; removes `AppUserRole.generalCoordinator` from all coordinator-gated nav item `roles` arrays, keeping only `coordination`, `coordinator`, and `administration` as the valid roles for restricted navigation items.",
  "frontend/src/components/ApprovalDetailView.tsx": "Read-only report detail view for coordinators with expandable activity cards and approve/return actions; now imports `Report` and `Activity` from `../types` instead of defining local interfaces, fixing the type mismatch with consuming pages.",
  "frontend/src/utils/consolidatedPdfGenerator.ts": "Generates a consolidated PDF report for all reports and activities; adds an `Impacto Esperado` narrative section to each report block in the HTML template, with local label helpers and summary statistics.",
  "frontend/src/utils/consolidatedCsvGenerator.ts": "CSV generator for consolidated report export; adds `Impacto Esperado` column to headers and data rows, with local museum/month/status label helpers and a TOTAL summary row.",
  "frontend/src/pages/ConsolidatedMuseumPage.tsx": "Consolidated museum report page; uses `MUSEUM_LOCATIONS` as a plain string array with `getMuseumLabel()` for display, fixing the `.value`/`.label` property errors.",
  "frontend/src/pages/ApprovalsPage.tsx": "Coordinator approvals page listing pending reports with search/status filters, approve confirmation dialog, and return-with-comment dialog; now imports `Report` and `Activity` from `../types` so the types match `ApprovalDetailView`'s props.",
  "frontend/src/components/dashboard/ReportsByMuseumChart.tsx": "Bar chart component visualizing report counts per museum using Recharts, with truncated X-axis labels, custom tooltip showing full museum name, and empty state handling.",
  "frontend/src/components/dashboard/ReportsByMonthChart.tsx": "Bar chart component visualizing report counts per month using Recharts, with months sorted chronologically using MONTH_ORDER, Portuguese month labels via getMonthLabel, and empty state handling.",
  "frontend/src/pages/UserManagementPage.tsx": "User management page with search, user list, edit dialog, and delete confirmation dialog; now fetches the current user's profile to conditionally show the delete button only to users with `administration`, `coordination`, or `coordinator` roles (matching the backend's `isCoordinator` authorization check for `deleteUserProfile`).",
  "frontend/src/components/ProfileSetupModal.tsx": "Dialog modal for first-time profile setup with required `onComplete` callback prop, team selection, and opaque bg-card backgrounds.",
  "frontend/src/pages/PendingApprovalPage.tsx": "Full-screen page shown to authenticated users whose accounts are awaiting coordinator approval; displays the Museus Centro logo, a clear pending-approval message with instructions, a re-send approval request button, and a logout button.",
  "frontend/src/index.css": "Global styles with OKLCH color system for light and dark modes, Plus Jakarta Sans font, utility component classes, and comprehensive global CSS overrides ensuring all native `<select>` elements and Radix UI Select/dropdown components render with a fully opaque white background (no transparency or backdrop-filter effects).",
  "frontend/tailwind.config.js": "Tailwind configuration with Plus Jakarta Sans display font, full OKLCH color token mapping including `success` and `warning` semantic colors, refined border radius, and custom shadow utilities.",
  "frontend/src/components/dashboard/PublicoGeralCard.tsx": "Dashboard card for Público Geral audience stats with mode selector; imports `AudienceQueryType` and `useGetTotalGeneralAudience` from useQueries (now correctly exported).",
  "frontend/src/components/MonthYearSelector.tsx": "Reusable month/year selector with `selectedYear: number` (not `number | ''`) and `onYearChange: (year: number) => void` to match usage in ReportsListPage.",
  "frontend/src/components/Layout.tsx": "Simple layout wrapper with Sidebar and Outlet; removes the `isApproved` prop from Sidebar since Sidebar no longer accepts that prop.",
  "frontend/src/components/dashboard/AudienceByProfileChart.tsx": "Bar/pie chart component visualizing audience breakdown by profile (children, youth, adults, elderly, PCD); removes the non-existent `AudienceBreakdown` import from backend and defines it as a local interface instead.",
  "frontend/src/components/ReportFilesSection.tsx": "Card component for managing file attachments linked to a report; replaces the missing `useListFiles` import (now correctly exported from useQueries).",
  "frontend/src/pages/FileManagementPage.tsx": "File management page for uploading, listing, downloading, and deleting files; replaces the missing `useListFiles` import (now correctly exported from useQueries).",
  "frontend/src/components/dashboard/KPICard.tsx": "Simple KPI card component accepting `number | string` value (no bigint) with locale-formatted number display."
}