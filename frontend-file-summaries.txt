{
  "frontend/src/App.tsx": "Main app with TanStack Router, auth guard using `useIsCallerApproved` (correct export name), profile setup modal, pending approval screen, sidebar layout, and all routes including activities new/edit routes.",
  "frontend/src/components/ActivitiesList.tsx": "Displays activities for a report as cards with status badges, hours, audience breakdown, classification, goal info, partnership, and cancellation reason; uses correct label function names (`activityStatusLabel`, `classificationLabel`, `productRealisedLabel`, `audienceRangeLabel`) from labels.ts.",
  "frontend/src/pages/ReportsListPage.tsx": "Reports list page with all existing functionality plus a new export section card containing a `MonthYearSelector` (defaulting to current month/year) and PDF/CSV export buttons; export handlers find the matching report by month/year, fetch its activities via `useGetReportWithActivities`, then call `generateReportPDF` or `generateExcelExport`, showing toast notifications for success/error/not-found cases.",
  "frontend/src/pages/ActivityFormPage.tsx": "Complete activity form page for creating and editing activities; replaces the free-text Profissional input with a shadcn/ui Select dropdown populated from `useListRegisteredProfessionals`, adds ACTION_TYPE_OPTIONS dropdown for Tipo de Ação, MUSEUM_OPTIONS dropdown for Local Realizado, and date/time fields for start/end; all dropdowns use explicit `bg-background border-border` styling to prevent transparency.",
  "frontend/src/pages/DashboardPage.tsx": "Coordination/Admin dashboard page with KPI cards, charts, filters, and export; fixes the `useIsCoordinadorGeral` call to pass no arguments.",
  "frontend/src/utils/pdfGenerator.ts": "Generates a formatted HTML document (opened in a new tab with auto-print) for a report and its activities; local getMuseumLabel maps all 6 MuseumLocation enum values including 'Equipe Principal' (renamed) and the 5 new team areas.",
  "frontend/src/utils/excelGenerator.ts": "CSV/Excel export generator with Portuguese column labels; adds a new `generateConsolidatedExcel(reports, activities)` convenience export that accepts a flat `Activity[]` array and builds the `activitiesByReport` Map internally before delegating to `generateExcelExport`.",
  "frontend/src/components/ExportReportPDFButton.tsx": "Simplified PDF export button that calls `generateReportPDF` (which now returns `void` and opens a new tab directly) instead of handling a Blob return value.",
  "frontend/src/components/ExportConsolidatedExcelButton.tsx": "Button (visible only to Administration role) that exports all reports and activities as a CSV file; fixes imports to use the renamed `useAllReports` and `useAllActivities` hooks.",
  "frontend/src/utils/labels.ts": "Utility module with label mapping functions for all backend enums; adds `getMonthOptions()`, `formatAudienceNumber()`, `getCurrentMonth()`, and `getCurrentYear()` helpers to support the new PublicoGeralCard component.",
  "frontend/src/components/SignatureCanvas.tsx": "Reusable HTML5 canvas component for electronic signature capture with mouse and touch support, a clear button, and an imperative handle to retrieve the base64 PNG signature.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; adds `useGetReport` as a backwards-compat alias for `useReport`, `useGetReportWithActivities` mutation for export, and other aliases `useGetReportsForUser`/`useGetActivitiesForReport` for compatibility.",
  "frontend/src/pages/ReportFormPage.tsx": "Complete report form page with tabs for the report form and an activities tab (when editing) that embeds ActivitiesList; supports create/update/submit flows with signature capture and role-based edit permissions.",
  "frontend/src/components/Sidebar.tsx": "Navigation sidebar with role-based nav items and user profile display; fixes the `useIsCoordinadorGeral` call to pass no arguments (the hook now reads the profile internally).",
  "frontend/src/components/ApprovalDetailView.tsx": "Read-only report detail view for Coordination/Admin with coordinator comments, signature canvas, and approve/return-for-revision actions; removes the broken `ExportReportPDFButton` usage (which requires `report` + `activities` props, not `reportId`) and uses correct `ExternalBlob` import directly.",
  "frontend/src/utils/consolidatedPdfGenerator.ts": "Generates a consolidated PDF report for all reports and activities; local getMuseumLocationLabel maps all 6 MuseumLocation enum values including 'Equipe Principal' and the 5 new team areas, with correct hours aggregation.",
  "frontend/src/utils/consolidatedCsvGenerator.ts": "CSV generator for consolidated report export; local getMuseumLocationLabel maps all 6 MuseumLocation enum values including 'Equipe Principal' and the 5 new team areas, with TOTAL row summing only numeric hours.",
  "frontend/src/pages/ConsolidatedMuseumPage.tsx": "Consolidated museum report page for Coordination/Admin; fixes imports to use the renamed `useAllReports` and `useAllActivities` hooks, and uses `MUSEUM_LOCATIONS` from labels for the museum selector.",
  "frontend/src/pages/ApprovalsPage.tsx": "Coordination/Admin-only page listing all reports with filter controls (status, month, search) and a detail sheet using ApprovalDetailView; passes userRole to ApprovalDetailView so the approve button respects role-based access control.",
  "frontend/src/components/dashboard/ReportsByMuseumChart.tsx": "Bar chart component visualizing report counts per museum using Recharts, with truncated X-axis labels, custom tooltip showing full museum name, and empty state handling.",
  "frontend/src/components/dashboard/ReportsByMonthChart.tsx": "Bar chart component visualizing report counts per month using Recharts, with months sorted chronologically using MONTH_ORDER, Portuguese month labels via getMonthLabel, and empty state handling.",
  "frontend/src/pages/UserManagementPage.tsx": "User management page for coordination/admin with pending approvals, user list, edit/delete dialogs, and role/museum editing; fixes the `useIsCoordinadorGeral` call to pass no arguments.",
  "frontend/src/components/ProfileSetupModal.tsx": "Dialog modal for first-time profile setup with name input and role selection; fixes the `saveProfile` call to include the required `museum: ''` field to satisfy the `UserProfile` type.",
  "frontend/src/pages/PendingApprovalPage.tsx": "Full-screen page shown to authenticated users whose accounts are awaiting coordinator approval; displays the Museus Centro logo, a clear pending-approval message with instructions, a re-send approval request button, and a logout button.",
  "frontend/src/index.css": "Global styles with OKLCH color system for light and dark modes, Plus Jakarta Sans display font, utility component classes, and global CSS overrides to ensure all dropdown/select/popover overlay components render with fully opaque backgrounds (no transparency or backdrop-filter effects).",
  "frontend/tailwind.config.js": "Tailwind configuration with Plus Jakarta Sans display font, full OKLCH color token mapping including `success` and `warning` semantic colors, refined border radius, and custom shadow utilities.",
  "frontend/src/components/dashboard/PublicoGeralCard.tsx": "Dashboard card for Público Geral audience stats; fixes `queryType` to always be non-null `AudienceQueryType` (never `null`) so it's compatible with `useGetTotalGeneralAudience`, and handles the `getCurrentMonth()` null return with a fallback.",
  "frontend/src/components/MonthYearSelector.tsx": "Reusable controlled month/year selector component using shadcn/ui Select dropdowns; accepts `selectedMonth`, `selectedYear`, `onMonthChange`, and `onYearChange` props, with Portuguese month labels from `getMonthOptions()` and year options from 2024–2027 (plus current year if not in range)."
}